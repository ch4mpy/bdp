name: rest-hero

services:
  reverse-proxy:
    container_name: rest-hero.reverse-proxy
    image: rest-hero.c4-soft.com/reverse-proxy
    volumes:
      - ./certs/${CN}.crt:/etc/nginx/certs/self_signed.crt
      - ./certs/${CN}.key:/etc/nginx/certs/self_signed_req_key.pem
    restart: unless-stopped
    ports:
      - 80:80
      - 443:443
    environment:
      TZ: "Pacific/Tahiti"
    extra_hosts:
      - host.docker.internal:host-gateway