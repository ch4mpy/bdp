# TP : conversions implicites

Les tests du customer-service ne passent pas.

- Identifier le problème.
- Expliquer pourquoi les tests `@WebMvcTest` du controller passent, mais pas le `@SpringBootTest` de l'application. 
- Implémenter la solution pour que tous les tests passent (sans modification des tests).