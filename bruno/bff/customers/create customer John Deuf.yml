info:
  name: create customer John Deuf
  type: http
  seq: 3

http:
  method: POST
  url: https://host.docker.internal/bff/m2m/customers
  body:
    type: json
    data: |-
      {
        "firstName": "John",
        "lastName": "Deuf",
        "birthDate": "1978-10-31",
        "birthLocation": "Longjumeau(91)"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
