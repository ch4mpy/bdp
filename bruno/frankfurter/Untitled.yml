info:
  name: Untitled
  type: http
  seq: 1

http:
  method: GET
  url: https://api.frankfurter.dev/v2/rate/EUR/AUD?providers=ECB
  params:
    - name: providers
      value: ECB
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
