info:
  name: list customers with firstOrLastNamePart
  type: http
  seq: 2

http:
  method: GET
  url: https://host.docker.internal:8083/customers?search=é
  params:
    - name: search
      value: é
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
